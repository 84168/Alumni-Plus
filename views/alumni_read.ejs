<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alumni</title>
    <style>
        .alumni_info {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            /* Adds spacing between cards */
            padding: 20px;
        }

        .alumni-card {
            flex: 0 0 calc(20% - 20px);
            /* 5 cards per row with spacing */
            max-width: 220px;
            background-color: #fff;
            border: 1px solid #e5e7eb;
            border-radius: 10px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
            padding: 16px;
            text-align: center;
            font-family: 'Segoe UI', sans-serif;
            transition: transform 0.2s ease;
        }

        .alumni-card:hover {
            transform: translateY(-4px);
        }

        .alumni-img {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 12px;
            border: 2px solid #3b82f6;
        }

        .alumni-name {
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 4px;
            color: #1f2937;
        }

        .alumni-batch {
            font-size: 13px;
            color: #6b7280;
            margin-bottom: 8px;
        }

        .alumni-email,
        .alumni-linkedin {
            font-size: 13px;
            margin-bottom: 6px;
        }

        .alumni-email a,
        .alumni-linkedin a {
            color: #2563eb;
            text-decoration: none;
        }

        .alumni-email a:hover,
        .alumni-linkedin a:hover {
            text-decoration: underline;
        }

        .popular-alumni-title {
            font-size: 24px;
            font-weight: 700;
            color: #1f2937;
            margin: 40px 0 20px;
            font-family: 'Segoe UI', sans-serif;
            position: relative;
        }

        .popular-alumni-title::after {
            content: '';
            display: block;
            width: 140px;
            height: 3px;
            background-color: #2563eb;
            /* Accent underline */
            margin-top: 2px;
            margin-left: 15px;
            border-radius: 2px;
        }

        /* Search Filter Container */
        .search-filter {
            /* background-color: rgba(255, 255, 255, 0.95); */
            padding: 24px;
            border-radius: 16px;
            /* box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15); */
            max-width: 600px;
            margin: 40px auto;
            z-index: 1;
            position: relative;
        }

        /* Form Layout */
        .search-filter form {
            display: flex;
            flex-wrap: wrap;
            gap: 16px;
            justify-content: center;
            align-items: center;
        }

        /* Large Name Input */
        #searchName {
            flex: 2;
            min-width: 250px;
            font-size: 15px;
            padding: 10px 14px;
            border: 1px solid #ccc;
            border-radius: 10px;
            transition: all 0.3s ease;
        }

        /* Small Batch Input */
        #batchFilter {
            flex: 1;
            min-width: 120px;
            font-size: 14px;
            padding: 8px 12px;
            border: 1px solid #ccc;
            border-radius: 10px;
            transition: all 0.3s ease;
        }

        /* Search Button */
        .search-filter button[type="submit"] {
            background-color: #4e54c8;
            color: #fff;
            border: none;
            border-radius: 10px;
            font-size: 14px;
            padding: 8px 20px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .search-filter button[type="submit"]:hover {
            background-color: #3b40a4;
        }

        /* Input Focus Effects */
        #searchName:focus,
        #batchFilter:focus {
            border-color: #4e54c8;
            box-shadow: 0 0 6px rgba(78, 84, 200, 0.4);
            outline: none;
        }

        /* Responsive Adjustments */
        @media (max-width: 640px) {
            .search-filter form {
                flex-direction: column;
                gap: 12px;
            }

            #searchName,
            #batchFilter,
            .search-filter button {
                width: 100%;
            }
        }

        h1 {
            font-size: 36px;
            font-weight: 700;
            text-align: center;
            margin-bottom: 40px;
            letter-spacing: 1.2px;
            background: linear-gradient(to right, #4e54c8, #8f94fb, #6a82fb, #a1c4fd);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
            animation: fadeInUp 1s ease-out;
            z-index: 1;
            position: relative;
        }

        .back-button {
            margin: 20px;
        }

        .back-button button {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 8px 16px;
            font-size: 16px;
            border-radius: 4px;
            cursor: pointer;
        }

        .back-button button:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <div class="back-button">
        <button onclick="history.back()" title="Go Back">&#8592; Back</button>
    </div>

    <h1>Explore Our Alumni Network</h1>

    <div class="search-filter mb-8 flex flex-col sm:flex-row items-center justify-between gap-4">
        <form action="/alumni_search" method="post">

            <input type="text" name="Name" id="searchName" placeholder="Search by name..."
                class="px-4 py-2 border rounded w-full sm:w-1/2">

            <input type="text" name="Batch" id="batchFilter" placeholder="Enter batch year..."
                class="px-4 py-2 border rounded w-full sm:w-1/4">

            <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">
                Search
            </button>
        </form>
    </div>

    <div class="popular-alumni-title">Popular Alumni</div>
    <div class="alumni_info">
        <% if(rows.length> 0) { %>
            <%rows.map((alumni)=>{ %>

                <div class="alumni-card">
                    <img src="https://static.vecteezy.com/system/resources/previews/019/896/008/non_2x/male-user-avatar-icon-in-flat-design-style-person-signs-illustration-png.png"
                        alt="Alumni Photo" class="alumni-img">

                    <h3 class="alumni-name">
                        <%=alumni.Full_Name%>
                    </h3>
                    <p class="alumni-batch">Batch: <%=alumni.Batch%>
                    </p>
                    <p class="alumni-email">📧 <a href="mailto:priya.sharma@example.com">
                            <%=alumni.Email_ID%>
                        </a></p>
                    <p class="alumni-linkedin">🔗 <a href="https://linkedin.com/in/priyasharma" target="_blank">LinkedIn
                            Profile</a>
                    </p>
                </div>

                <%}) %>
                    <% } else { %>

                        <%}%>
    </div>

</body>

</html>