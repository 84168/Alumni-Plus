<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mentorship Requests to Alumni</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px 20px;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 50px;
            color: white;
        }

        .header h1 {
            font-size: 42px;
            font-weight: 700;
            margin-bottom: 12px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }

        .header p {
            font-size: 18px;
            opacity: 0.95;
            font-weight: 300;
        }

        .cards-container {
            display: flex;
            flex-direction: column;
            gap: 40px;
            padding: 20px;
            max-width: 65%;
            margin: 0 auto;
        }

        .request-card {
            background: white;
            border-radius: 24px;
            padding: 0;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            overflow: hidden;
            position: relative;
        }

        .request-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
        }

        .request-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.4);
        }

        .card-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 35px;
            color: white;
            position: relative;
            overflow: hidden;
        }

        .card-header::after {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
            animation: pulse 3s ease-in-out infinite;
        }

        @keyframes pulse {

            0%,
            100% {
                transform: scale(1);
                opacity: 0.5;
            }

            50% {
                transform: scale(1.1);
                opacity: 0.8;
            }
        }

        .request-title {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 12px;
            position: relative;
            z-index: 1;
        }

        .request-subtitle {
            font-size: 14px;
            opacity: 0.9;
            font-weight: 400;
            position: relative;
            z-index: 1;
        }

        .card-body {
            padding: 35px;
        }

        .alumni-section {
            background: linear-gradient(135deg, #f8f9ff 0%, #f0f2ff 100%);
            padding: 25px;
            border-radius: 16px;
            margin-bottom: 30px;
            border: 2px solid #e8ebff;
            position: relative;
        }

        .alumni-section::before {
            content: '👤';
            position: absolute;
            top: -15px;
            left: 25px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 8px 14px;
            border-radius: 12px;
            font-size: 20px;
        }

        .alumni-section h3 {
            color: #667eea;
            font-size: 18px;
            margin-bottom: 15px;
            margin-top: 10px;
            font-weight: 600;
        }

        .alumni-info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
        }

        .info-item {
            display: flex;
            flex-direction: column;
        }

        .info-label {
            font-size: 11px;
            text-transform: uppercase;
            color: #8b8b8b;
            font-weight: 600;
            letter-spacing: 1px;
            margin-bottom: 4px;
        }

        .info-value {
            font-size: 15px;
            color: #333;
            font-weight: 500;
        }

        .description-section {
            margin-bottom: 30px;
        }

        .description-section h3 {
            color: #333;
            font-size: 16px;
            margin-bottom: 12px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .description-text {
            color: #555;
            line-height: 1.8;
            font-size: 15px;
        }

        .details-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin-bottom: 30px;
        }

        .detail-card {
            background: #fafbff;
            padding: 20px;
            border-radius: 14px;
            border: 1px solid #e8ebff;
            transition: all 0.3s;
        }

        .detail-card:hover {
            background: #f3f5ff;
            border-color: #d0d5ff;
            transform: translateY(-2px);
        }

        .detail-icon {
            font-size: 28px;
            margin-bottom: 10px;
        }

        .detail-label {
            font-size: 11px;
            text-transform: uppercase;
            color: #8b8b8b;
            font-weight: 600;
            letter-spacing: 1px;
            margin-bottom: 6px;
        }

        .detail-value {
            font-size: 16px;
            color: #333;
            font-weight: 600;
        }

        .mode-badge {
            display: inline-block;
            padding: 6px 14px;
            border-radius: 20px;
            font-size: 13px;
            font-weight: 600;
            margin-top: 8px;
        }

        .mode-online {
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
            color: #1976d2;
        }

        .mode-offline {
            background: linear-gradient(135deg, #fce4ec 0%, #f8bbd0 100%);
            color: #c2185b;
        }

        .status-section {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 14px;
            margin-bottom: 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .status-info {
            display: flex;
            flex-direction: column;
        }

        .status-label {
            font-size: 12px;
            color: #8b8b8b;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .status-badge {
            display: inline-block;
            padding: 8px 18px;
            border-radius: 20px;
            font-size: 13px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .status-pending {
            background: linear-gradient(135deg, #fff3cd 0%, #ffe69c 100%);
            color: #856404;
            box-shadow: 0 4px 10px rgba(255, 193, 7, 0.3);
        }

        .status-accepted {
            background: linear-gradient(135deg, #d4edda 0%, #c3e6cb 100%);
            color: #155724;
            box-shadow: 0 4px 10px rgba(40, 167, 69, 0.3);
        }

        .status-rejected {
            background: linear-gradient(135deg, #f8d7da 0%, #f5c6cb 100%);
            color: #721c24;
            box-shadow: 0 4px 10px rgba(220, 53, 69, 0.3);
        }

        .action-buttons {
            display: flex;
            gap: 12px;
        }

        .btn {
            padding: 14px 28px;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            font-size: 15px;
            font-weight: 600;
            transition: all 0.3s;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            box-shadow: 0 8px 20px rgba(40, 167, 69, 0.4);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 28px rgba(40, 167, 69, 0.5);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
            color: white;
            box-shadow: 0 8px 20px rgba(220, 53, 69, 0.4);
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 28px rgba(220, 53, 69, 0.5);
        }

        @media (max-width: 1200px) {
            .cards-container {
                max-width: 85%;
            }
        }

        @media (max-width: 768px) {
            .cards-container {
                max-width: 100%;
            }

            .details-grid {
                grid-template-columns: 1fr;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }
        }
        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(135deg, #f8f9ff 0%, #f0f2ff 100%);
            border-radius: 16px;
            border: 2px dashed #d0d5ff;
        }

        .empty-state-icon {
            font-size: 64px;
            margin-bottom: 16px;
            opacity: 0.6;
        }

        .empty-state-title {
            font-size: 20px;
            font-weight: 600;
            color: #555;
            margin-bottom: 8px;
        }

        .empty-state-message {
            font-size: 14px;
            color: #888;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .mentorship-cards-grid {
                grid-template-columns: 1fr;
            }

            .card-details {
                grid-template-columns: 1fr;
            }

            .card-actions {
                flex-direction: column;
            }
        }

        .hidden {
            display: none;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>📚 Mentorship Session Requests</h1>
            <p>Requests sent to alumni for conducting mentorship sessions</p>
        </div>

        <div class="cards-container">
            <% if(sessionReq && sessionReq.length>0) {%>


                <%sessionReq.forEach((req)=>{%>
                    <!-- Request Card 1 -->
                    <div class="request-card">
                        <div class="card-header">
                            <div class="request-title" id="title-<%= req.Session_ID %>">
                                <%=req.Topic%>
                            </div>
                            <div class="request-subtitle" id="CreatedOn-<%= req.Session_ID %>">Request sent on
                                <%=req.Created_On%>
                            </div>
                        </div>

                        <div class="card-body">
                            <div class="description-section">
                                <h3>📝 Session Description</h3>
                                <div class="description-text" id="description-<%= req.Session_ID %>">
                                    <%=req.Description%>
                                </div>
                            </div>

                            <div class="details-grid">
                                <div class="detail-card">
                                    <div class="detail-icon">📅</div>
                                    <div class="detail-label">Scheduled Date</div>
                                    <div class="detail-value" id="schedule-<%= req.Session_ID %>">
                                        <%=req.Session_Date%>
                                    </div>
                                </div>

                                <!-- <div class="detail-card">
                                <div class="detail-icon">🕐</div>
                                <div class="detail-label">Time Slot</div>
                                <div class="detail-value"></div>
                            </div> -->

                                <div class="detail-card">
                                    <div class="detail-icon">⏱️</div>
                                    <div class="detail-label">Duration</div>
                                    <div class="detail-value" id="duration-<%= req.Session_ID %>">
                                        <%=req.Duration%>
                                    </div>
                                </div>

                                <!-- <div class="detail-card">
                                <div class="detail-icon">👥</div>
                                <div class="detail-label">Max Participants</div>
                                <div class="detail-value">30 Students</div>
                            </div> -->

                                <div class="detail-card">
                                    <div class="detail-icon">💻</div>
                                    <div class="detail-label">Session Mode</div>
                                    <div class="detail-value">
                                        Online
                                        <div class="mode-badge mode-online" id="mode-<%= req.Session_ID %>">
                                            <%=req.Mode%>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="action-buttons">
                                <button class="btn btn-primary" id="accept-<%= req.Session_ID %>"
                                    onclick="handleAccept('<%= req.Session_ID %>')">Accept
                                    Request</button>
                                <button class="btn btn-secondary" id="reject-<%= req.Session_ID %>"
                                    onclick="handleReject('<%= req.Session_ID %>')">Reject
                                    Request</button>
                            </div>
                        </div>
                    </div>

                    <%})%>
                        <% } else { %>
                            <div class="empty-state">
                                <div class="empty-state-icon">✅</div>
                                <h3 class="empty-state-title">Currently No Requests</h3>
                                <p class="empty-state-message">To be a part of Mentor's activate the mentorship option in edit profile.</p>
                                <!-- <p class="empty-state-message">If already done wait for Admin to send request you for mentorship.</p> -->
                            </div>
                            <%}%>


        </div>
    </div>
    <script>
        function handleReject(sessionId) {
            window.location.href = `/rejectMentorshipRequest/${sessionId}`;
        }
        function handleAccept(sessionId) {
            // Redirect to your desired route, passing sessionId as a query or path param
            window.location.href = `/acceptMentorshipRequest/${sessionId}`;
        }
        const alertMessage = "<%= alertMessage %>";
        if (alertMessage && alertMessage.trim() !== "") {
            alert(alertMessage);
        }
    </script>
</body>

</html>